apiVersion: apps/v1
kind: Deployment
metadata:
  name: control
  labels:
    k8s-app: wireflow-control
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: wireflow-control
  template:
    metadata:
      labels:
        k8s-app: wireflow-control
    spec:
      hostAliases:
      - ip: "10.7.88.27"
        hostnames:
          - "signaling.wireflow.run"
      serviceAccountName: wireflow-controller-admin
      initContainers:
        - name: wait-for-nats
          image: curlimages/curl:latest
          command:
            - '/bin/sh'
            - '-c'
            - |
              until curl -f http://wireflow-nats-service:8222/varz; do
                echo "Waiting for nats server..."
                sleep 2
              done
      containers:
        - name: wireflow-control
          image: manager:latest
          imagePullPolicy: Always
          args:
            - "start"
            - "manager"
            - "--level=INFO"
            - "--env=prod"
          volumeMounts:
            - mountPath: /app/deploy/conf.yaml
              name: conf
              subPath: conf.yaml
          env:
            # 对应结构体 Database.DSN
            - name: APP_DATABASE_DSN
              value: "root:root@tcp(10.7.88.27:3306)/wireflow?charset=utf8mb4&parseTime=True&loc=Local"

            # 对应结构体 Dex.Issuer
            - name: APP_DEX_ISSUR
              value: "http://wireflow-dex.wireflow-system.svc.cluster.local:5556/dex/auth"
            # 对应结构体 Dex.Issuer
            - name: APP_DEX_PROVIDERURL
              value: "http://wireflow-dex.wireflow-system.svc.cluster.local:5556/dex"
      volumes:
        - name: conf
          configMap:
            name: wireflow-control-config
            items:
              - key: conf.yaml
                path: conf.yaml
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: turn
  labels:
    k8s-app: turn
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: wireflow-turn
  template:
    metadata:
      labels:
        k8s-app: wireflow-turn
    spec:
      hostAliases:
        - ip: "10.7.88.27"
          hostnames:
            - "signaling.wireflow.run"
      initContainers:
        - name: wait-for-nats
          image: curlimages/curl:latest
          command:
            - '/bin/sh'
            - '-c'
            - |
              until curl -f http://wireflow-nats-service:8222/varz; do
                echo "Waiting for nats server..."
                sleep 2
              done
      containers:
        - name: wireflow-turn
          image: manager:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3478
              protocol: UDP
              hostPort: 3478
          args:
            - "start"
            - "turn"
            - "--level=debug"
            - "--public-ip=81.68.109.143"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wrrper
  labels:
    k8s-app: wireflow-wrrper
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: wireflow-wrrper
  template:
    metadata:
      labels:
        k8s-app: wireflow-wrrper
    spec:
      hostAliases:
        - ip: "81.68.109.143"
          hostnames:
            - "signaling.wireflow.run"
      serviceAccountName: wireflow-controller-admin
      initContainers:
        - name: wait-for-nats
          image: curlimages/curl:latest
          command:
            - '/bin/sh'
            - '-c'
            - |
              until curl -f http://wireflow-nats-service:8222/varz; do
                echo "Waiting for nats server..."
                sleep 2
              done
      containers:
        - name: wireflow-wrrper
          image: manager:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 6266
              hostPort: 6266
          args:
            - "start"
            - "wrrper"
            - "--listen=:6266"
            - "--level=INFO"
          volumeMounts:
            - mountPath: /app/deploy/conf.yaml
              name: conf
              subPath: conf.yaml
      volumes:
        - name: conf
          configMap:
            name: wireflow-control-config
            items:
              - key: conf.yaml
                path: conf.yaml
