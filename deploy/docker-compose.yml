services:
  wf-agent:
    image: ${WF_IMAGE:-ghcr.io/wireflowio/wireflowd:latest}
    container_name: wf-agent
    privileged: true
    network_mode: "host"
    restart: always
    command:
      - up
      - --token=${WF_TOKEN}
      - --server-url=${WF_SERVER_URL}
      - --signaling-url=${WF_SIGNALING_URL}
      - --vm-endpoint=${VM_ENDPOINT}/api/v1/write
      - --enable-metric
      - --save
