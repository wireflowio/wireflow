services:
  lat-agent:
    image: ${WF_IMAGE:-ghcr.io/latticeio/latticed:latest}
    container_name: lat-agent
    privileged: true
    network_mode: "host"
    restart: always
    command:
      - up
      - --token=${WF_TOKEN}
      - --server-url=${WF_SERVER_URL}
      - --signaling-url=${WF_SIGNALING_URL}
      - --vm-endpoint=${VM_ENDPOINT}/api/v1/write
      - --enable-metric
      - --save
